spring:
  data.redis:
    repositories:
      enabled: true
    host: ${REDIS_HOST}
    username: ${REDIS_USERNAME}
    password: ${REDIS_PASSWORD}
    port: ${REDIS_PORT}
    database: 1
    lettuce:
      pool:
        enabled: true